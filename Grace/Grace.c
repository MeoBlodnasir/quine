#include <stdio.h>
/*
	com
*/
#define a
#define b
#define m(x) int main(){FILE*file=fopen("Grace_kid.c", "w");char*str="#include <stdio.h>%c/*%c%ccom%c*/%c#define a%c#define b%c#define m(x) int main(){FILE*file=fopen(%cGrace_kid.c%c, %cw%c);char*str=%c%s%c;fprintf(file, str, 10, 10, 9, 10, 10, 10, 10, 34, 34, 34, 34, 34, str, 34, 10, 10);return(0);}%cm(x)%c";fprintf(file, str, 10, 10, 9, 10, 10, 10, 10, 34, 34, 34, 34, 34, str, 34, 10, 10);return(0);}
m(x)
